{
  "name": "competency-phase2",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "docker compose up",
    "down": "docker compose down",
    "build": "docker compose build --no-cache && docker compose up -d"
  }
}